# AGENTS Instructions

## Required Workflow

- Before making any code or configuration change, read this `AGENTS.md` file first.
- After every modification, verify the affected project can compile or build successfully.
- If compilation or build fails after a change, fix the issue before finishing the task unless blocked by an external dependency or environment problem.
